# Strings factory presets

- Device: OP-XY
- Firmware catalogue: 1.1.21
- Source track: 8

Each project contains one pattern with zero notes on Track 8. Only the named
factory Strings preset was loaded before the project was saved on the device.
The default `draemy` state is already present on Track 7 of a clean
device-authored default project, so this batch contains the other six presets.

Capture order is alphabetical:

1. `ensemble.xy`
2. `intimate str.xy`
3. `nachtmusik.xy`
4. `pointe.xy`
5. `soutenu.xy`
6. `whitness.xy`
